To install:
add to /etc/apache2/sites-available/000-default.conf 
right before </VirtualHost>

<Proxy *>
	Allow from all
</Proxy>
ProxyPass /loopsocket http://localhost:3100/


run:
sudo bash ./deploy.sh